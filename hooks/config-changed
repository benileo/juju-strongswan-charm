#!/usr/bin/env python3

from strongswan.sysctl import(
	sysctl
)
from strongswan.iptables import(
	iptables
)
from charmhelpers.core import (
	hookenv
)

config = hookenv.config()



def _update():
	sysctl()
	iptables()

if __name__ == "__main__":
	hookenv.log("INFO\tRunning the configuration changed hook")
	_update()
	hookenv.log("INFO\tRunning the configuration changed hook")

